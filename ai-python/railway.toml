[build]
buildCommand = "pip install -r requirements.txt && curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && apt-get install -y nodejs && npm install -g @mermaid-js/mermaid-cli"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"